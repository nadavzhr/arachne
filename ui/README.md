# Arachne UI

Static React + Vite dashboard for viewing exported job data.

## Run locally

```bash
npm install
npm run dev
```

## Data source

The UI reads from public/jobs.json, generated by:

```bash
uv run arachne export
```

For GitHub Pages, make sure the exported jobs.json is committed in ui/public/.
