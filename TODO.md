# TODO

1. Apple is pretty much done, paginated, returns results (though it varies from time to time)
might be because of dedups.. check it out later but not mandatory
4. Fix Amazon results (currently return 95 which is way higher than what I see manually (59))

5. Paginate Google

* Trim the base crawler model to only include shared (and relevant) fields.

* Consider renaming "Sources" to "Crawlers" or "Spiders".

